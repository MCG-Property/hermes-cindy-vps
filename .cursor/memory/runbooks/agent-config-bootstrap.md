# Runbook: agent configuration / bootstrap

- **Domain:** agent configuration / bootstrap
- **Purpose:** Record execution of `.cursor/BOOTSTRAP.md` reconciliation.
- **Procedure:** Materialize missing `.cursor/` paths per `BOOTSTRAP.md` §4; validate §5. See linked spec: `.cursor/BOOTSTRAP.md`.
- **Date:** 2026-05-04 (UTC inferred from session)
- **Validation:** Tree matches `BOOTSTRAP.md` §3; empty `memory/memories/`, `memory/blockers/`, `memory/blockers-fixed/` tracked via `.gitkeep`.

---
